1. Add tests
2. Add typescript
3. Add access and refresh token
4. To cut the bd operation from routes and pass it to services or controllers
5. Add error handler (routes, status, validation, mongoose, mongo)
6. Add Actions table to mongodb
7. Add koa-compose for middleware
8. Configuration, conf file, dev, prod, test

# How past variables to process.env
# Modeling MongoDB database
# {Разобраться с импортами }
# add error message constants
