# Add tests
# Add typescript
# Add access and refresh token
# To cut the bd operation from routes and pass it to services or controllers
# Add error handler (routes, status, validation, mongoose, mongo)
# Add Actions table to mongodb
# Add koa-compose for middleware
# Configuration, conf file, dev, prod, test
# Add permission
# Add checking permission (when try to get smth)
---------------------------------------------
# How past variables to process.env
# Modeling MongoDB database
# { Разобраться с импортами }
# Add error message constants 
